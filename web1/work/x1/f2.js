

/**
 * Define a function called "log" that takes two arguments and prints the first
 * parameter prefixed by the string "log: " and appends the second parameter
 * enclosed in square brackets "[]"
 * i.e. if test "test" and "message" were the first and second parameters, log
 * would output:
 * "log: test [message]"
 */




/**
 * Define a function called "vprint" that takes three parameters.  Call the
 * log function above with the first and third parameters and then again with
 * the second and third parameters.
 */




vprint(process.argv[2], process.argv[3], process.argv[4]);
