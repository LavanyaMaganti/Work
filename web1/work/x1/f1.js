#!/usr/local/bin/node

/**
 * Complete the function bar below.  Make it take three parameters and return
 * the result of multiplying the first and third and subtracting the second.
 */



// Use the parseInt function to convert the first command line parameter
// (process.argv[2]) to an integer and store it in the variable n.


// Call bar() define above twice with n, n+1 and n+2 as the parameters.
// multiply the first bar(...) by the second bar(..) call, store the result
// in the variable r:


// Use the console.log() function to print out the value in r:

