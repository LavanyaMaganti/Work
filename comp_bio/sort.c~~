#include<stdio.h>
#include<math.h>
int main(){
  int n,i;
        printf("Enter n value\n");
        scanf("%d",&n);
        printf("Enter array values\n");
int arr[n];
        for(i=0;i<n;i++)
        scanf("%d",&arr[i]);

int smallest_1=arr[0];
int smallest_2=arr[1];
int smallest_3=arr[2];

   for(int i=0;i<n;i+=3) {
      /*for(int j=i;j<n;j++){
        if(arr[i]<arr[j]){
          smallest=arr[i];
          arr[i]=arr[j];
          arr[j]=smallest;
        }
*/
      if(arr[i]<arr[i+1] && arr[i]<arr[i+2]){
      smallest_1=arr[i];
      }
      else if(arr[i+1]<arr[i] && arr[i+1]<arr[i+2]){
      smallest
      }
  
      }
    }
       // for(i=0;i<n;i++)
      //  printf("%d\t",arr[i]);

        printf("Third smallest number %d \n",arr[n-3]);
return 0;
}
